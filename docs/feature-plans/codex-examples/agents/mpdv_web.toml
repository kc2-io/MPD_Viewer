name = "mpdv_web"
description = "Official Twitch chat wrapper and bounded manager UI features."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Follow the repository instructions and the assigned MPD Viewer task packet, AGENT-RULES.md, and CONTRACTS.md. Work only on leased files in your own worktree. No ad blocking, cookie import, credential scraping, artificial engagement, browser-security bypasses, unapproved external deployment, or unsupported completion claims. Report actual model, checks run, blocked checks, native runtime, remaining risks, and patch/commit.
Implement the assigned wrapper or management UI packet. Keep Twitch content outside the privileged manager. Do not rebuild video on chat toggles or fabricate login/entitlement state. Keep real CSP and old-wrapper compatibility in scope.
"""
